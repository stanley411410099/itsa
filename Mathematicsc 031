#include <stdio.h>

int main() {
    int N, i, sum = 0;

    scanf("%d", &N);

    for (i = 1; i <= N; i++) {
        if (i % 6 == 0 && i % 12 != 0)
            sum += i;
    }

    printf("%d\n", sum);

    return 0;
}
